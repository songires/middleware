import React from 'react';
import { Button } from 'react-toolbox/lib/button';
import theme from './SubmitButton.scss';

const SubmitButton = (props) => <Button {...props} theme={theme} />;
export default SubmitButton;
